<div class="container-fluid">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2 articlecolumn">
      <% @articles.each do |article| %>
        <h2><%= link_to article.title, article %></h2>
        <p><%= article.content %></p>
      <% end %>
    </div>
  </div>
</div>
